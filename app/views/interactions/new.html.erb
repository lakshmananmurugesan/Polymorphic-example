<% if @context.class == Business %>

    <h1>Editing Interaction for <%= @context.name %></h1>

<% else %>

    <h1>Editing Interaction for <%= @context.first_name %></h1>

<% end %>

<%= render "form" %>